{% extends 'base.html.twig' %}

{% block title %}Attestation{% endblock %}

{% block body %}
    <h1>Attestation</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ attestation.id }}</td>
            </tr>
            <tr>
                <th>Etudiant</th>
                <td>{{ attestation.etudiant }}</td>
            </tr>
            <tr>
                <th>Convention</th>
                <td>{{ attestation.convention }}</td>
            </tr>
            <tr>
                <th>Message</th>
                <td>{{ attestation.message }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_attestation_index') }}">Retour à la liste</a>

    <a href="{{ path('app_attestation_edit', {'id': attestation.id}) }}">Modifier</a>

    {{ include('attestation/_delete_form.html.twig') }}
{% endblock %}
